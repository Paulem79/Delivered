# These are read from the real environment too: a .env file is optional.
# Default port for the server to listen on
PORT=3000
# Address to bind to (use 127.0.0.1 when behind a reverse proxy)
BIND_ADDR=0.0.0.0
# Files directory path
FILES_DIR=./files
